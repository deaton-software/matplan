# Published data snapshot — lives in the repo but is written by the in-app
# Publish flow via the GitHub API, never committed from a local working copy.
matplan-data.json

# Local archives / OS cruft
*.zip
.DS_Store
Thumbs.db
